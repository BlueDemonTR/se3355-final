import { ContentArea, Input } from 'components'
import React, { useState } from 'react'

const Auth = ({  }) => {

  return (
    <ContentArea>
      <Input />
    </ContentArea>
  )
}

export default Auth